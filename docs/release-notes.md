# ReaWebAPI

## Changes

- Add `reaper.transaction.batch(callback)` with Mirror signatures, automatic result references, tuple destructuring and typed result selection for the 173 reviewed batch APIs. Preserve call-array batches, native errors and ordinary Mirror calls. Update the Demo and TypeScript example.

## 更新

- 新增 `reaper.transaction.batch(callback)`，沿用 Mirror 签名，支持 173 项已审核批处理 API、自动结果引用、多返回值解构和结果类型推导。保留调用数组、原生错误及普通 Mirror 调用，同步更新 Demo 和 TypeScript 示例。
